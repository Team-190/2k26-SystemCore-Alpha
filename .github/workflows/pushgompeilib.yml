name: Push GompeiLib

on:
  release:
    types: [published]

jobs:
  sync:
    uses: Team-190/.github/.github/workflows/syncgompeilib.yaml@main
    with:
      direction: push
    secrets:
      GOMPEILIB_SYNC: ${{ secrets.GOMPEILIB_SYNC }}